import 'package:flutter/material.dart';

class FixeroSearchBar extends StatelessWidget {
  const FixeroSearchBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
